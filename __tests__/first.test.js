// import "react-native";
// import React from "react";
// import renderer from "react-test-renderer";

describe("App snapshot", () => {
  jest.useFakeTimers();
  beforeEach(() => {});

  it("renders the loading screen", async () => {
    expect("a").toBe("a");
  });
  it("renders the s screen", async () => {
    expect("a").toBe("a");
  });
});
